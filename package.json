{
  "name": "car-customizer-app",
  "scripts": {
    "start": "npm run local",
    "local": "cd app && MONGO_URL=mongodb://localhost:27017/car-customizer meteor --settings .deploy/local.json --port 3000"
  }
}
